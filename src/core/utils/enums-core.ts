export enum TEST_EXECUTE {
    localExecutor = "localExecutor",
    ciExecutor = "ciExecutor",
    remoteExecutor = "remoteExecutor",
}

export enum BROWSER {
    chromium = "chromium",
    firefox = "firefox",
    webkit = "webkit",
    mobileChromium = "mobileChromium",
    mobileWebkit = "mobileWebkit",
}